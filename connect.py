# """This file contains necessary variables required to establish a connection to an SQLite database 
# and defines cursor to navigate through and operate on record sets"""

# import sqlite3 as sql # import the module and assign it with alias

# dbCon = sql.connect("filmflix.db") #connecting to the database

# dbCursor = dbCon.cursor() #create variable passing it to the cursor method

